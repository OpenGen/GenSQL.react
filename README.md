# GenSQL.react

## Install dependencies

```shell
pnpm install 
```

## Development

To get the right version of pnpm/npm, type:

```shell 
nix-shell
```

To run the Storybook type:

```shell
pnpm run storybook
```
